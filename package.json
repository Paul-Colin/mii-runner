{
  "name": "mii-genetic-runner",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "build": "docker compose build"
  }
}